name: Service Test Pipeline
on:
 pull_request:
 push:
  
jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v1
    - name: List of Requested Revierwers
      env:
        GITHUB_CONTEXT: ${{ toJson(github.event.pull_request.requested_reviewers) }}              
      run: echo "$GITHUB_CONTEXT"
      if: contains(${{ toJson(github.event.pull_request.requested_reviewers) }}.*.login, 'shubhamkarnwal12331313')
    - name: Execute if reviewer found                              
      run: echo "Reviewer Found"

     
 
 
